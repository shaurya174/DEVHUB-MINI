<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100">
  <form action="/edit/<%= post.id %>" method="POST" class="bg-white p-10 rounded-2xl shadow-lg w-full max-w-lg">
    <h1 class="text-2xl font-bold mb-6 text-indigo-600">Edit Post</h1>

    <div class="mb-4">
      <label class="block text-gray-700 font-semibold mb-2" for="title">Title</label>
      <input type="text" name="title" id="title" value="<%= post.title %>" required 
        class="w-full px-4 py-2 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500">
    </div>

    <div class="mb-4">
      <label class="block text-gray-700 font-semibold mb-2" for="content">Content</label>
      <textarea name="content" id="content" required rows="6"
        class="w-full px-4 py-2 border rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500"><%= post.content %></textarea>
    </div>

    <div class="flex justify-end gap-3">
      <a href="/" class="bg-red-500 text-white px-5 py-2 rounded-xl font-semibold hover:bg-red-400 transition">Cancel</a>
      <button type="submit" class="bg-indigo-600 text-white px-6 py-2 rounded-xl font-semibold hover:bg-indigo-500 transition">Save Changes</button>
    </div>
  </form>
</body>
</html>
